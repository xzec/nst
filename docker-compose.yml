services:

  db:
    image: postgres:18
    restart: always
    shm_size: 128mb
    volumes:
      - pg-data:/var/lib/postgresql
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '${DB_PORT}:5432'

volumes:
  pg-data:
